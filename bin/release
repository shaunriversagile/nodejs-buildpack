#!/usr/bin/env bash
# bin/release <build-dir>
echo "--- Release"
echo "  PATH: $PATH:/home/vcap/app/bin"

cat << EOF
addons: []
default_process_types:
  console: "nohup Xvfb &"
  web: npm start
EOF

echo "--- Release"
echo "  PATH: $PATH:/home/vcap/app/bin"
