#!/usr/bin/env bash
# bin/release <build-dir>

cat << EOF
addons: []
default_process_types:
  console: "nohup Xvfb &"
  web: export PATH=/home/vcap/app/bin:$PATH; pwd; ls -ltr /home/vcap/app/bin; npm start
EOF
